// import useToggle from "@rooks/use-toggle";

// const customToggleFunction = (v) => (v === "start" ? "stop" : "start");

const Comp = () => {
  //   const [value2, toggleValue2] = useToggle(true);
  //   const [value3, toggleValue3] = useToggle("start", customToggleFunction);

  return (
    <div>
      <h1>Comp here</h1>
      <h1>for testing purpose only</h1>

      {/*<section>
        <h3>Initial true</h3>
        <button onClick={toggleValue2}>
          {value2.toString() === "false" ? "1" : "2"}
        </button>
        <hr />
      </section>
      <section>
        <h3 className="bg-white">Custom values</h3>
        <button onClick={toggleValue3}>{value3}</button>
      </section>*/}
    </div>
  );
};

export default Comp;
